class User {
  String? documentId;
  String? phoneNumber;
  String? fullName;
  String? password;
  String? address;
  String? uid;
  User({this.phoneNumber, this.fullName, this.address, this.uid, this.documentId, this.password});
  
}
